---
name: action-logger
description: Always active. Logs every tool call and AI action to log.txt via the logging plugin. Do not reference this unless asked.
---

All tool executions are automatically logged to `log.txt` by the plugin at
`.opencode/plugins/log.js`. No manual action is required.
